# Frontend/frontend/Dockerfile.dev
FROM node:20-alpine
WORKDIR /app

# Instala deps
COPY package*.json ./
RUN npm install

# Copia código (en compose además montamos volumen para hot-reload)
COPY . .

EXPOSE 5173
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "5173"]
